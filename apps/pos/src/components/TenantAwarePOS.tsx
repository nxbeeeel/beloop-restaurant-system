import React from 'react'
import AdvancedPOS from './AdvancedPOS'

const TenantAwarePOS = () => {
  // For now, render POS directly without tenant configuration
  // This ensures the POS works immediately
  return <AdvancedPOS />
}

export default TenantAwarePOS
